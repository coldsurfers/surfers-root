pre-commit:
  parallel: true
  jobs:
    - run: pnpm biome check --write {staged_files}
      glob: "*.{js,ts,jsx,tsx}"
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: npx commitlint --edit
