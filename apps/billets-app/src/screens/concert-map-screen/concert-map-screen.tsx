import { CommonScreenLayout } from '@/ui'
import MapView from 'react-native-maps'

export const ConcertMapScreen = () => {
  return (
    <CommonScreenLayout>
      <MapView></MapView>
    </CommonScreenLayout>
  )
}
