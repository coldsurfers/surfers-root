'use client';

import { AnimatedForm } from './animated-form';

export const StoreRegistrationUserVoiceForm = () => {
  return (
    <AnimatedForm title="전하고 싶은 의견이 있으시면, 입력해주세요" onSubmit={() => {}}>
      StoreRegistrationUserVoiceForm
    </AnimatedForm>
  );
};
